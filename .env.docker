# Variables de entorno para Docker Compose
# MongoDB Configuration (usa el nombre del servicio 'mongodb' en lugar de localhost)
MONGO_URI=mongodb://mongodb:27017
DB_NAME=document_classifier

# Document Storage Path (dentro del contenedor)
DOCUMENT_ROOT=/app/Documentos

# JWT Configuration
SECRET_KEY=tu_clave_secreta_super_segura_cambiala
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
